# Requirements

# In order to use this application, the user must have python (Python 3+) and streamlit installed (version 0.60.0 here)
# and the following packages with at least current versions :

requests==2.23.0
json==2.0.9
numpy==1.18.1
pandas==1.0.3
matplotlib==3.1.1
